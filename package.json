{
  "name": "kubeconform-action",
  "version": "1.0.0",
  "description": "Add .github/workflows/kubeconform.yml with the following contents:",
  "main": "index.js",
  "scripts": {
    "build": "tsc && ncc build -o dist/run/ src/main.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "@types/node": "^18.13.0"
  },
  "dependencies": {
    "@actions/cache": "^3.1.3",
    "@actions/tool-cache": "^2.0.1",
    "@vercel/ncc": "^0.36.1",
    "typescript": "^4.9.5"
  }
}
